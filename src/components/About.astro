<section id="about" class="about">
	<div class="container">
		<h2>Sobre mí</h2>
		
		<div class="content">
			<div class="text">
				<p>
					Soy <strong>Alejandro Sánchez García</strong>, estudiante de 
					<strong>Ingeniería en Sistemas</strong> en la 
					<strong>Universidad Mexiquense del Bicentenario</strong>, apasionado 
					por el desarrollo de software, las tecnologías, los videojuegos y los fps.
				</p>
				<p>
					He trabajado en <strong>desarrollo Full Stack</strong> con React, 
					JavaScript y Python. También exploro el <strong>desarrollo de videojuegos</strong> 
					con Godot, creando practicas que me ayudan a mejorar.
				</p>
				<p>
					Además del código, tengo habilidades en <strong>diseño digital</strong> 
					para juegos 2D, lo que me permite crear proyectos completos.
				</p>
				<p>
					Mi objetivo es seguir creciendo como desarrollador, aprendiendo nuevas 
					tecnologías y contribuyendo a proyectos innovadores.
				</p>
				<p>
					Tambien me gusta la creacion de contenido en YouTube sobre videojuegos en general.
				</p>
			</div>
			
			<div class="stats">
				<div class="stat">
					<div class="number">+3</div>
					<div class="label">Proyectos</div>
				</div>
				<div class="stat">
					<div class="number">10+</div>
					<div class="label">Tecnologías</div>
				</div>
				<div class="stat">
					<div class="number">100%</div>
					<div class="label">Dedicación</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	/* Sección sobre mí */
	.about {
		padding: 6rem 2rem;
		background: #0a0a0a;
		position: relative;
	}
	.about::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(90deg, transparent, rgba(148, 163, 184, 0.4), transparent);
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	/* Título de sección */
	h2 {
		font-size: clamp(2.5rem, 5vw, 4rem);
		text-align: center;
		margin-bottom: 4rem;
		color: #f8fafc;
		position: relative;
		font-weight: 800;
		text-shadow: 0 0 30px rgba(148, 163, 184, 0.3);
	}

	/* Decoración */
	h2::after {
		content: '';
		display: block;
		width: 100px;
		height: 4px;
		background: linear-gradient(135deg, #94a3b8, #cbd5e1, #f1f5f9);
		margin: 1.5rem auto 0;
		border-radius: 2px;
		box-shadow: 0 0 20px rgba(148, 163, 184, 0.5);
	}

	.content {
		display: grid;
		gap: 4rem;
	}

	/* Párrafos */
	.text p {
		color: #cbd5e1;
		line-height: 1.9;
		margin-bottom: 1.5rem;
		font-size: 1.125rem;
	}

	/* Texto strong */
	.text strong {
		color: #f8fafc;
		font-weight: 600;
		background: linear-gradient(135deg, #94a3b8, #cbd5e1, #f1f5f9);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		text-shadow: 0 2px 10px rgba(148, 163, 184, 0.3);
	}

	/* Estadísticas */
	.stats {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
		margin-top: 2rem;
	}

	/* Tarjetas */
	.stat {
		background: linear-gradient(135deg, rgba(15, 15, 15, 0.95), rgba(30, 30, 30, 0.95));
		border: 1px solid rgba(148, 163, 184, 0.3);
		padding: 2.5rem 2rem;
		border-radius: 20px;
		text-align: center;
		transition: all 0.3s;
		backdrop-filter: blur(10px);
		position: relative;
		overflow: hidden;
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5), inset 0 1px 2px rgba(255, 255, 255, 0.05);
	}

	/* Efecto de brillo*/
	.stat::before {
		content: '';
		position: absolute;
		top: -50%;
		left: -50%;
		width: 200%;
		height: 200%;
		background: radial-gradient(circle, rgba(241, 245, 249, 0.1) 0%, transparent 70%);
		opacity: 0;
		transition: opacity 0.3s;
	}

	.stat:hover::before {
		opacity: 1;
	}

	.stat:hover {
		transform: translateY(-8px);
		border-color: rgba(203, 213, 225, 0.6);
		box-shadow: 0 12px 30px rgba(148, 163, 184, 0.4), 0 0 50px rgba(203, 213, 225, 0.2);
		background: linear-gradient(135deg, rgba(20, 20, 20, 0.95), rgba(40, 40, 40, 0.95));
	}

	.icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	/* Números de estadísticas */
	.number {
		font-size: 3rem;
		font-weight: 800;
		margin-bottom: 0.5rem;
		background: linear-gradient(135deg, #f8fafc, #cbd5e1, #f1f5f9);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		text-shadow: 0 0 30px rgba(203, 213, 225, 0.3);
		position: relative;
	}

	/* Efecto de resplandor*/
	.number::after {
		content: attr(data-number);
		position: absolute;
		left: 0;
		top: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(135deg, #94a3b8, #cbd5e1);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		filter: blur(15px);
		opacity: 0.6;
		z-index: -1;
	}

	.label {
		font-size: 1rem;
		color: #94a3b8;
		font-weight: 500;
		letter-spacing: 0.5px;
	}

	/* Responsive */
	@media (max-width: 640px) {
		.stats {
			grid-template-columns: 1fr;
		}
	}
</style>