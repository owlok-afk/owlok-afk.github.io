<section id="skills" class="skills">
	<div class="container">
		<h2>Habilidades</h2>
		
		<div class="grid">
			<div class="category">
				<h3>Frontend</h3>
				<div class="items">
					<span>HTML/CSS</span>
					<span>JavaScript</span>
					<span>React</span>
					<span>Astro</span>
				</div>
			</div>
			
			<div class="category">
				<h3>Backend</h3>
				<div class="items">
					<span>Node.js</span>
					<span>Python</span>
					<span>APIs</span>
					<span>Bases de Datos</span>
				</div>
			</div>
			
			<div class="category">
				<h3>Game Dev</h3>
				<div class="items">
					<span>Godot Engine</span>
					<span>2D Design</span>
					<span>Pixel Art</span>
					<span>Game Mechanics</span>
				</div>
			</div>
			
			<div class="category">
				<h3>Herramientas</h3>
				<div class="items">
					<span>GitHub</span>
					<span>VS Code</span>
					<span>Linux</span>
					<span>Docker</span>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	/* Sección de habilidades*/
	.skills {
		padding: 6rem 2rem;
		background: #000000;
		position: relative;
	}
	.skills::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(90deg, transparent, rgba(148, 163, 184, 0.4), transparent);
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	/* Título de sección*/
	h2 {
		font-size: clamp(2.5rem, 5vw, 4rem);
		text-align: center;
		margin-bottom: 4rem;
		color: #f8fafc;
		position: relative;
		font-weight: 800;
		text-shadow: 0 0 30px rgba(148, 163, 184, 0.3);
	}

	/* Decoración debajo del título*/
	h2::after {
		content: '';
		display: block;
		width: 100px;
		height: 4px;
		background: linear-gradient(135deg, #94a3b8, #cbd5e1, #f1f5f9);
		margin: 1.5rem auto 0;
		border-radius: 2px;
		box-shadow: 0 0 20px rgba(148, 163, 184, 0.5);
	}

	/* Grid responsive*/
	.grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 2rem;
	}

	/* Tarjetas de categoría*/
	.category {
		background: linear-gradient(135deg, rgba(15, 15, 15, 0.95), rgba(30, 30, 30, 0.95));
		border: 1px solid rgba(148, 163, 184, 0.3);
		padding: 2.5rem 2rem;
		border-radius: 20px;
		transition: all 0.3s;
		backdrop-filter: blur(10px);
		position: relative;
		overflow: hidden;
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5), inset 0 1px 2px rgba(255, 255, 255, 0.05);
	}

	/* Efecto de brillo metálico*/
	.category::before {
		content: '';
		position: absolute;
		top: -50%;
		left: -50%;
		width: 200%;
		height: 200%;
		background: radial-gradient(circle, rgba(241, 245, 249, 0.08) 0%, transparent 70%);
		opacity: 0;
		transition: opacity 0.3s;
	}

	.category:hover::before {
		opacity: 1;
	}

	/* Efecto hover  */
	.category:hover {
		transform: translateY(-8px);
		border-color: rgba(203, 213, 225, 0.6);
		box-shadow: 0 12px 30px rgba(148, 163, 184, 0.4), 0 0 50px rgba(203, 213, 225, 0.2);
		background: linear-gradient(135deg, rgba(20, 20, 20, 0.95), rgba(40, 40, 40, 0.95));
	}

	.icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	/* Títulos */
	h3 {
		color: #f8fafc;
		font-size: 1.5rem;
		margin-bottom: 1.5rem;
		font-weight: 700;
		background: linear-gradient(135deg, #f8fafc, #cbd5e1);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		text-shadow: 0 2px 15px rgba(148, 163, 184, 0.3);
	}

	/* Contenedor de tags */
	.items {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	/* Tags individuales de habilidades con estilo metálico */
	.items span {
		background: linear-gradient(135deg, rgba(148, 163, 184, 0.1), rgba(203, 213, 225, 0.1));
		border: 1px solid rgba(148, 163, 184, 0.3);
		color: #e2e8f0;
		padding: 0.6rem 1.2rem;
		border-radius: 25px;
		font-size: 0.9rem;
		font-weight: 500;
		transition: all 0.3s;
		position: relative;
		overflow: hidden;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3), inset 0 1px 2px rgba(255, 255, 255, 0.05);
	}

	/* Efecto de brillo en los tags */
	.items span::before {
		content: '';
		position: absolute;
		top: -50%;
		left: -50%;
		width: 200%;
		height: 200%;
		background: radial-gradient(circle, rgba(241, 245, 249, 0.15) 0%, transparent 60%);
		opacity: 0;
		transition: opacity 0.3s;
	}

	.items span:hover::before {
		opacity: 1;
	}

	/* Hover en tags*/
	.items span:hover {
		background: linear-gradient(135deg, rgba(148, 163, 184, 0.25), rgba(203, 213, 225, 0.25));
		border-color: rgba(203, 213, 225, 0.6);
		color: #f8fafc;
		transform: translateY(-2px);
		box-shadow: 0 6px 15px rgba(148, 163, 184, 0.5), 0 0 25px rgba(203, 213, 225, 0.2);
	}

	/* Responsive */
	@media (max-width: 768px) {
		.grid {
			grid-template-columns: 1fr;
		}
	}
</style>